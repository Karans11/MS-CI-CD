# Intentionally insecure configuration
server.port=8082
spring.application.name=payment-service

# Hardcoded database credentials
spring.datasource.url=jdbc:mysql://mysql-db:3306/payments
spring.datasource.username=root
spring.datasource.password=RootPassword123!
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Hardcoded payment gateway credentials (intentionally insecure)
payment.gateway.api.key=pk_test_fake_key_12345678
payment.gateway.secret=sk_test_fake_secret_87654321
payment.gateway.webhook.url=https://payment-gateway.fake.com/webhook

# Exposed sensitive endpoints
management.endpoints.web.exposure.include=*
debug=true
logging.level.root=DEBUG

# Weak encryption
payment.encryption.key=12345
payment.encryption.algorithm=DES
